# Capsules — the corpus rollup

Generated by grounding-wiki `scripts/capsules.mjs` at corpus commit ad1085047fbf413d249818b651dcb224725409e3.
Derived state (docs/corpus-spec.md, "The capsule tier and CAPSULES.md") — do not edit by
hand; regenerate whenever any note's `description:` changes:

    node ${CLAUDE_PLUGIN_ROOT}/scripts/capsules.mjs <repo-root> docs/wiki

- [[overview]] — the system's shape: two planes (ingest job, query path), the container
  tarrow's system shape — an ingest job and a query path over PostGIS, packaged as a container composition, built so under-restriction and address retention are structurally impossible rather than avoided by care; entry point for the two data planes and the three structural gates.

- [[constitution-and-principles]] — the seven principles the code is written against,
  The seven constitutional principles tarrow is written against, and the specific file or gate in the repository that enforces each one rather than asserting it.

- [[http-envelope]] — the server process, its response headers, the CSP, and the error
  The Node server that owns the HTTP port, the security headers set on every response, the two-policy CSP split — a per-response nonce for rendered documents, a script-free policy everywhere else — and the error paths built so they cannot carry query context.

- [[process-output-seal]] — why the request process is made incapable of writing to
  After startup the request process replaces stdout, stderr, and every console method with discards, so no dependency can log a searched address — a property of the process rather than an audit of the code.

- [[search-orchestration]] — the control flow of answering one address: manifest first,
  The control flow of answering one address — manifest first, refuse on empty layers, resolve, decline without a parcel, measure pessimistically, resolve ambiguity to the most restrictive candidate.

- [[result-type-gate]] — the closed result union and the compile-time assertions that
  The closed result union plus compile-time assertions that reject any variant, reason, basis, or field name reading as permission — making an unqualified clearance structurally inexpressible rather than merely absent.

- [[coverage-manifest]] — what was and was not checked, read from data on every answer,
  What was and was not checked, read from the database on every answer and mandatory on every result variant — including a gate that refuses to build a manifest without the rule-content disclosure.

- [[address-resolution]] — normalize, match against county address points, attribute a
  Normalizing a typed address and matching it against county address points, then attributing a parcel — with fuzzy matching and coarse fallbacks made structurally impossible rather than merely unused.

- [[proximity-measurement]] — nearest boundary to nearest boundary in a projected metre
  The spatial query measuring nearest parcel boundary to nearest parcel boundary in a projected metre CRS, subtracting uncertainty radii so flagging is always the more likely outcome.

- [[measurement-uncertainty]] — where the radii come from, and why they are SQL functions
  The three SQL functions holding the buffer and both uncertainty radii, written exactly once so the measurement cannot drift from the disclosure of the measurement.

- [[database-access]] — the read-only pool, the file-loaded query registry, and why query
  The application's single read-only connection pool and the startup loader that reads every spatial query from a file, so query text is never assembled in TypeScript.

- [[web-surface]] — routes, the rule that nothing load-bearing hides behind hydration, and
  The React Router 7 routes, the CSS-only card deck the answer is delivered as, the rule that nothing load-bearing may hide behind hydration even though first-party script now ships, and how a POST keeps the searched address out of every URL, history entry, and proxy log.

- [[answer-rendering]] — how a result becomes a page without the renderer deciding
  How a search result becomes a deck of full-screen cards — the five copy rules, the collapse rule that keeps limitations visible while provenance folds away, the one-line coverage statement beside the finding, and the prohibition on the renderer computing anything.

- [[ingest-pipeline]] — the ETL job: fetch, stage, truncate-and-reload, assert, stamp.
  The ETL job — fetch each source to NDJSON, stage with a server-side COPY, rebuild every derived table by truncate-and-reload, assert, and stamp layer freshness.

- [[data-sources]] — the layers ingested, why each one, and what each is known not to
  The five fetched layers and two derived school layers, why each was chosen so their weaknesses do not coincide, and what each is known not to cover.

- [[coverage-gap-ledger]] — how known absences become rows that every answer renders.
  The table where every known absence becomes a row every answer renders — declared gaps authored in code, per-row gaps discovered during a load, tarrow's disclosure of its own unverified rule, and the two-audience split between a short label and the full record.

- [[ingest-assertions]] — the checks that abort a load rather than let it finish quietly.
  Load-time checks that abort the ingest rather than warn — key usability, row accounting, mandatory gap rows for null geometry, and a guard against the schema growing an assumed school radius.

- [[database-schema]] — the migration set, the derived tables, and the grant that makes
  The migration set applied in filename order by a one-shot runner — the derived spatial tables, the read-only runtime role, and the authored SQL functions compiled in as build output.

- [[container-composition]] — services, profiles, volumes; the container as the only
  The compose file that is the entire environment — db, migrate, app as the production set, plus etl, test, browser-test and spike behind profiles, with nothing installed on a host.

- [[database-logging-posture]] — the PostgreSQL flags that keep the searched address out
  The PostgreSQL command-line flags that keep the searched address out of the database's own logs, chosen as argv flags rather than an init script so they apply unconditionally and are readable from outside.

- [[test-suite]] — what the suite proves, why it runs only in a container, and why some
  What the suite proves, why it runs only inside a container, why a passing run with zero collected tests is treated as a failure, and why the log-capture test is given the Docker socket.

- [[privacy-verification]] — the published procedure for checking the privacy claims from
  The published procedure for checking tarrow's privacy claims from outside the containers — the six claims, the nonce and script checks, how to make each check fail, and the interception bound the checks deliberately do not cover.

- [[self-hosting]] — the published-image composition, and what it refuses to start without.
  The deployment composition — pinned published images, credentials that must be supplied rather than defaulted, no published database port, and a CI job that keeps its logging flags identical to the development file.

- [[work-planning]] — specs, the Backlog board, one-task-one-PR, and where decisions and
  How work is proposed and landed — Spec Kit specs, the Backlog.md board with its fixed label taxonomy, one TASK one PR, and where frozen spike evidence and runbooks live.
