{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "PATH=\"${VOLTA_HOME:-$HOME/.volta}/bin:/opt/homebrew/bin:/usr/local/bin:$PATH\"; command -v node >/dev/null || { echo 'root-guard: node not found on PATH — guard cannot run' >&2; exit 2; }; node \"$CLAUDE_PROJECT_DIR/.claude/hooks/root-guard-hook.mjs\" pre-bash"
          }
        ]
      },
      {
        "matcher": "Write|Edit|NotebookEdit",
        "hooks": [
          {
            "type": "command",
            "command": "PATH=\"${VOLTA_HOME:-$HOME/.volta}/bin:/opt/homebrew/bin:/usr/local/bin:$PATH\"; command -v node >/dev/null || { echo 'root-guard: node not found on PATH — guard cannot run' >&2; exit 2; }; node \"$CLAUDE_PROJECT_DIR/.claude/hooks/root-guard-hook.mjs\" pre-write"
          }
        ]
      }
    ]
  }
}
