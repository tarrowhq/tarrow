export default function Index() {
  return (
    <main>
      <h1>somap</h1>
      <p>
        Walking skeleton, Phase 1 (TASK-0002.01): the container composition —
        database, migrations, and this server — is up. The address search
        itself is built in later phases of this task.
      </p>
    </main>
  );
}
