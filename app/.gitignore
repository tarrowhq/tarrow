# Installed by npm ci inside the container; never on the host, never committed.
node_modules/

# Vite/RR7 build output, regenerated by `npm run build`.
build/

# react-router typegen output, regenerated by `npm run typecheck`.
.react-router/
