---
id: TASK-0001
title: 'Spike: choose a geocoding and distance stack that does not leak user addresses'
status: In Progress
assignee: []
created_date: '2026-08-04 15:58'
updated_date: '2026-08-04 17:59'
labels:
  - 'area:geo'
  - 'kind:spike'
  - 'x:privacy'
milestone: m-0
dependencies: []
priority: high
ordinal: 1000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Decide how somap turns a typed address into coordinates, and how it measures distance, WITHOUT sending the user's searched address to a third party.

This is the first real collision between the product and Constitution Principle III, and it is likely the highest-risk unknown in the project. A commercial geocoder (Google, Mapbox) is the easy path and is disqualifying on its face: the searched address is exactly the future-intent datum the constitution says must never leave the user's control, and handing it to an ad company is worse than logging it ourselves.

Evaluate self-hosted options (Nominatim, Pelias, libpostal plus a local address dataset) against coverage quality for Summit County, operational cost, and update cadence. Address coverage quality is a safety concern, not just a UX one: a geocoder that silently resolves an unknown address to a ZIP centroid produces a confidently wrong distance, which is the under-restriction failure of Principle I.

Ships a decision document. The application is untouched, so this satisfies the deployability rule trivially.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 Candidate geocoders evaluated against Summit County addresses with a documented accuracy sample
- [ ] #2 Chosen stack sends no user-entered address to any third party, and this is demonstrable
- [ ] #3 Failure behavior specified: an address that cannot be confidently geocoded returns an explicit could-not-locate, never a low-confidence coordinate
- [ ] #4 Distance measurement method documented, including what geometry is used when parcel boundaries are unavailable
- [ ] #5 Decision and rationale committed to the repo
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
ARCHITECTURE DECIDED (session 2026-08-04). Empirical work (AC #1) and the
decision document (AC #5) remain.

1. PARCEL-FIRST, NOT POINT-FIRST
   Resolve the typed address to its PARCEL POLYGON from the Summit County
   Fiscal Office parcel dataset -- not to a coordinate. Measure polygon
   boundary to polygon boundary.
   Basis: ORC 2950.034 defines protected "premises" as "the parcel of real
   property on which [the facility] is situated" (explicit for preschool/
   child care, children's crisis care, and residential infant care; school
   premises is incorporated by reference from elsewhere in the code). A
   point-based geocode is therefore an APPROXIMATION of the legal standard,
   not an implementation of it -- and it errs under-restrictively on large
   campuses and corner lots, which is the non-recoverable direction.
   Bonus: one parcel ingest serves BOTH this geocoder and the school-premises
   geometry TASK-0002.02 needs. One pipeline, two consumers.

2. MEASUREMENT: NEAREST BOUNDARY TO NEAREST BOUNDARY
   The statute does NOT specify a measurement method. Secondary sources say
   property line to property line. We do not need to resolve the ambiguity to
   act safely: nearest-boundary distance is always <= centroid distance, so
   the boundary reading flags a STRICT SUPERSET of addresses. If the property-
   line reading is legally correct we are right; if a laxer reading is
   permitted we were over-restrictive, which Principle I classifies as
   recoverable and a disclosure item. TASK-0003 verifies the legal basis.

3. UNCERTAINTY IS ARITHMETIC, NOT VIBES
   Every geometry carries an uncertainty radius. Comparisons use the
   pessimistic bound: d_min = d(a,b) - r_a - r_b; flag if d_min < buffer.
   This is Principle I as a formula, and it is renderable per Principle II
   ("parcel boundary to parcel boundary, exact" vs "interpolated street
   position, +/-80m").

4. ONE ENGINE, TWO DELIVERY MODES (hybrid)
   PostGIS is the ONLY thing that ever computes an answer. Two deliveries:
   (a) a PRECOMPUTED INDEX -- run the query over every county parcel at build
       time, ship the index, client looks up locally. No network request, so
       privacy is structural rather than policy.
   (b) a SERVER QUERY ENDPOINT -- conventional, no-log, for anyone who
       prefers it or falls outside a precomputed jurisdiction.
   The index is a PROJECTION of the engine, generated by it. This is the same
   shape Principle IV sets for rules (files -> ETL -> DB -> now also index):
   everything downstream of the files is derived, disposable, rebuilt in full.
   No reconciliation, so the fast path CANNOT disagree with the slow path.
   TASK-0002.01 survives -- PostGIS's role sharpens from "query server" to
   "computation core both modes are built from."
   The coverage manifest MUST state which path answered (Principle II).

5. FALLBACK: DECLINE ONLY
   No parcel match => explicit could-not-locate (AC #3). No map pin, no
   nearby-parcel context, no degraded coordinate. There is no ZIP-centroid
   path to fall down because the only returnable thing is a parcel or nothing.
   CONSEQUENCE: this makes the PARCEL MATCH RATE load-bearing. Decline-only is
   honest, but a 15% miss rate sends a sixth of users back to calling the
   sheriff. Measuring it is the empirical core of AC #1, not a checkbox.

6. MATCHING: libpostal AS NORMALIZER, BOTH SIDES
   Not as a geocoder. Normalize the parcel table's address fields at ingest,
   normalize user input at query, match normalized forms. Parcel address
   fields are inconsistent about directionals, abbreviations, unit suffixes.
   Unit numbers are DISCARDED: every unit in a building shares the parcel and
   is therefore at the same distance. Multi-unit addresses stop being a
   matching problem.

7. SELF-HOSTING IS NOW CONSTITUTIONAL
   Ratified as Principle VII (constitution 1.1.0, PR #2). Every input freely
   redistributable; no managed service in the query path; nothing licensed per
   query; packaging is a deliverable; a self-hosted instance must surface its
   own data build date and rule verification dates so it can say it is stale.

REMAINING WORK
- AC #1: obtain the Fiscal Office parcel dataset; build the normalize+match
  prototype; run a documented accuracy sample over real Summit County
  addresses; report match rate AND a failure taxonomy.
- AC #4: pin the geometry rule for facilities with NO parcel available (a
  licensed daycare known only by address) -- an assumed premises radius per
  facility class, applied via the rule in item 3.
- AC #5: write the decision document.

OUT OF SCOPE, HANDED OFF
- ORC 2950.034 conditions preschool/child-care premises on the premises being
  PROPERLY MARKED with signage. Whether a given facility is posted is not
  knowable from GIS data. Principle I says assume it IS marked (restrict) and
  disclose. Flag on TASK-0003; NOT resolved here.
- Retroactivity limits on 2950.034 are rule content -> TASK-0003.
<!-- SECTION:PLAN:END -->
